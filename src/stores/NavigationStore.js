import { defineStore } from "pinia";

export const getNavigationStore = defineStore("navigation", {
  state: () => ({
    showNavigationBar: true,
    showFooter: true
  }),
  getters: {
    
  },
  actions: {
    
  }
});
